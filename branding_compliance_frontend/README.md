# Lightweight React Template for KAVIA

This project provides a minimal React template with a clean, modern UI and minimal dependencies.

Now extended with an Elegant "Royal Purple" themed flow to upload PDFs and logos, poll status, preview results, and download the corrected PDF.

## Environment

Create a `.env` file (or set environment variables in your deployment) to point the frontend to the backend:

```
REACT_APP_BACKEND_URL=http://localhost:3001
```

See `.env.example` for reference.

## Getting Started

In the project directory, you can run:

### `npm start`

Runs the app in development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

### `npm test`

Launches the test runner in interactive watch mode.

### `npm run build`

Builds the app for production to the `build` folder.\
It correctly bundles React in production mode and optimizes the build for the best performance.

## Learn More

To learn React, check out the [React documentation](https://reactjs.org/).
